models:
  - name: silver
    columns:
      - name: country_name
        datatest:
          - unique
          - not null
      - name: official_name
        datatest:
          - relationships:
              to: ref('dim_customers') # Test for referential integrity
              field: customer_id

