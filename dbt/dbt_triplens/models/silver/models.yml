models:
  - name: silver
    columns:
      - name: country_name
        test:
          - unique
          - not null
      - name: official_name
        test:
          - relationships:
              to: ref('dim_customers') # Test for referential integrity
              field: customer_id

